import SwiftUI

@main
struct UpdatePilotApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .defaultSize(width: 900, height: 640)
        .windowStyle(.hiddenTitleBar)
    }
}
